//nodemon will reload the server after each change of index js -- we installed it and called it in scripts in package.json with the command start:
npm start is possible now
//with sequelize helps us to interact with the data base easier
it created to folders config and models
in models you can create the tables directly from there
sequelize will name the table automaticly like the file name